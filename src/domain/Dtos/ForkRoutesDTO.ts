export interface IForkRoutesDTO {
    destination : string,
    arrive : string,
    previousArrive : string,
    routeJoin : string[],
    routeInvalid : string,
    finalDestination : string,
    price: number[]
}
